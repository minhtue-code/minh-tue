const users = ["Cong", " Tue", "Minh", "Hai", "Dũng", "Hoang"];

const User = [];

for (let i = users.length - 1; i >= 0; i--) {
  User.push(users[i]);
}
for (let i = 0; i < users / length; i++) {
  User2.unshift(users[i]);
}

console.log(User);
console.log(User2);
